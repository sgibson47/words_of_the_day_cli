require 'spec_helper'

describe WordsOfTheDay do
  it 'has a version number' do
    expect(WordsOfTheDay::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
